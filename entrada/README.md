# entrada/ — fora do git

Os originais que o médico manda (foto, logotipo, documento) ficam aqui, numa pasta por slug:
`entrada/drjoao/retrato.jpg`. Nada daqui vai para o repositório nem para o ar: a versão publicada sai
de `npm run imagens` para `site/<slug>/imagens/`, sem metadados.
